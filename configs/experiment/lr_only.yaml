# @package _global_
# Pipeline A experiment: Logistic Regression only

training:
  pipeline: lr
  smote: true
  cv_folds: 5
  random_state: 42

model:
  lr:
    max_iter: 1000
    class_weight: balanced
