version: '3.8'

services:
  mlops_frauddetection:
    build:
      context: .
      dockerfile: dockerfiles/Dockerfile
    container_name: mlops_frauddetection_app
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8000:8000"
